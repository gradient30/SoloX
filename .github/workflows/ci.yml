name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  test:
    name: Test on ${{ matrix.os }} Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        # macOS runner  scarce：仅保留 3.11 冒烟；Linux/Windows 仍跑全版本矩阵。
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12']
        include:
          - os: macos-latest
            python-version: '3.11'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov pytest-mock pytest-timeout pyyaml
    
    - name: Verify setup.py dependencies
      run: python scripts/verify_setup.py
    
    - name: Lint with flake8
      run: |
        pip install flake8
        # stop the build if there are Python syntax errors or undefined names
        flake8 solox/ --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings
        flake8 solox/ --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics
    
    - name: Validate compatibility matrix
      run: python scripts/validate_compatibility_matrix.py

    - name: Test with pytest
      env:
        OBJC_DISABLE_INITIALIZE_FORK_SAFETY: YES
      run: |
        pytest tests/ -v --cov=solox --cov-report=xml --cov-report=term --timeout=180
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  dependency-check:
    name: Dependency Security Check
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install safety
      run: pip install safety
    
    - name: Check dependencies for security vulnerabilities
      run: |
        pip install -r requirements.txt
        safety check
      continue-on-error: true

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
    
    - name: Build package
      run: python -m build
    
    - name: Check package
      run: twine check dist/*
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: dist
        path: dist/

  docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      run: |
        docker build -t solox:latest .
      continue-on-error: true  # Docker 构建可能需要额外配置

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [test, build]
    if: github.event_name == 'release' && github.event.action == 'published'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
    
    - name: Build package
      run: python -m build
    
    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: twine upload dist/*

  documentation:
    name: Check Documentation
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Check documentation files
      run: |
        echo "📚 检查文档文件..."
        
        # 检查必需的文档文件
        required_docs=(
          "README.md"
          "docs/README.md"
          "docs/DEPENDENCIES.md"
          "docs/compatibility-matrix.md"
          "docs/06-engineering/project-layout.md"
          "docs/06-engineering/release-and-dev-standards.md"
          "docs/06-engineering/pre-publish-checklist.md"
          "docs/acceptance/joint-review-2026-compatibility.md"
          "docs/acceptance/l3-device-lab-checklist.md"
          "docs/02-development/quick-start.md"
          "docs/05-issues/troubleshooting.md"
          "scripts/README.md"
          "runtime/README.md"
        )
        
        for doc in "${required_docs[@]}"; do
          if [ -f "$doc" ]; then
            echo "✅ $doc 存在"
          else
            echo "❌ $doc 缺失"
            exit 1
          fi
        done
        
        echo "✅ 所有必需文档文件都存在"
    
    - name: Check scripts
      run: |
        echo "🔧 检查脚本文件..."
        
        required_scripts=(
          "scripts/install_dependencies.sh"
          "scripts/install_dependencies.ps1"
          "scripts/verify_setup.py"
          "scripts/validate_compatibility_matrix.py"
          "scripts/release_gate.sh"
          "scripts/release_gate.ps1"
          "scripts/dev.sh"
        )
        
        for script in "${required_scripts[@]}"; do
          if [ -f "$script" ]; then
            echo "✅ $script 存在"
          else
            echo "❌ $script 缺失"
            exit 1
          fi
        done
        
        echo "✅ 所有必需脚本文件都存在"
